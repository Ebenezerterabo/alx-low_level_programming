#include "main.h"

/**
 *_abs - A function that computes the absolute value of an integer
 *Return: Always 0
 *@m: the integer used
 */

int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	else

		return (-m);

}
