#include "main.h"

/**
 * reverse_array - A function that reverses the content of an array
 * of integers.
 *
 * @a: integer 1
 * @n: integer 2
 *
 *Return: void
 */

void reverse_array(int *a, int n)
{
	int x = 0, y, tmp;

	for (y = n - 1; y > x; y--)
	{
		tmp = a[b];

		a[y] = a[x];

		a[x] = tmp;

		x++;

	}

}


