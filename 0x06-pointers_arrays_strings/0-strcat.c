#include "main.h"

/**
 * _strcat - A function that concatenates two strings
 * @dest: pointer 1
 * @src: pointer 2
 * Return: Always 0
 */

char *_strcat(char *dest, char *src)
{
	int x, y;

	for (x = 0; dest[a] != '\0'; x++)
		;

		for (y = 0; src[b] != '\0'; y++)
		{
			dest[a + b];
		}

		return (dest);

}
