#include "main.h"

/**
 *add - A function that adds two integers and returns the result.
 *@r: int to be added to r
 *@s: int to be added to s
 *Return: Always q (Success)
 */

int add(int r, int s)
{
	int q;

	q = r + s;

	return (q);

}
